# PostgreSQL / Neon
# Use the pooled URL at runtime and the direct URL for migrations when your provider supplies both.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# Optional but recommended for `prisma migrate dev` on a managed PostgreSQL database.
SHADOW_DATABASE_URL="postgresql://USER:PASSWORD@HOST/SHADOW_DATABASE?sslmode=require"
