import { Building2, Mail, MapPin, Phone } from "lucide-react";
import { orders } from "@/lib/admin/orders-data";

export default function ClientsPage() {
  return <main className="mx-auto w-full max-w-[1500px] p-4 md:p-7 xl:p-9"><div><p className="text-[10px] font-black uppercase tracking-[0.22em] text-orange-600">Base clients</p><h1 className="mt-2 text-3xl font-black md:text-4xl">Clients</h1><p className="mt-2 text-sm text-slate-500">Retrouvez les entreprises ayant commandé sur OYSTE.</p></div><div className="mt-7 grid gap-4 lg:grid-cols-2">{orders.map((order) => <article key={order.id} className="rounded-[1.4rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-slate-100 text-slate-500"><Building2 size={21} /></div><div><h2 className="text-lg font-black">{order.customer.company}</h2><p className="mt-1 text-xs font-bold text-slate-500">{order.customer.name}</p></div></div><div className="mt-5 grid gap-3 text-xs text-slate-600 sm:grid-cols-2"><p className="flex items-center gap-2"><Mail size={15} className="text-slate-400" />{order.customer.email}</p><p className="flex items-center gap-2"><Phone size={15} className="text-slate-400" />{order.customer.phone}</p><p className="flex items-center gap-2"><MapPin size={15} className="text-slate-400" />{order.customer.city}</p><p className="font-black text-[#007f8f]">1 commande · {order.family}</p></div></article>)}</div></main>;
}
